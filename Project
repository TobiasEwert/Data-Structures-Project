#include <iostream>
#include <string>
#include <vector>
using namespace std;

struct Edge {
  int dist;
  int cost;
};

struct Vertex {
  string airport;
  string state;
  bool visited;

};

class Graph {
  Graph() {}

  void insert_vertex(const Vertex<T>& ver) {

  }

  void add_edge(const Vertex<T>& origin, const Vertex<T>& dest, int d, int c) {

  }

  

};

int main() {
  ifstream infile("airports.txt");
  string origin, dest, destState;
  string line;
  int dist, cost;

  while(getline(infile, line)) {

  }

  //Create graph object
  
}
